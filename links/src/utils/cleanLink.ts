
type CleanLink = {
	(link: string, site?: string): string;
}

/**
 *
 * @param link
 * @param site
 * @returns
 */
export const cleanLink: CleanLink = (link, site) => {
	// TODO: clean the given link
	return link;
}
