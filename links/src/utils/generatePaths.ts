
type GeneratePaths = {
	(links: any, site?: string): string[];
}

/**
 * Generates the path based on links from Storyblok
 * @param links
 * @param site
 * @returns
 */
export const generatePaths: GeneratePaths = (links, site) => {
	// TODO: logic
	return []
}
